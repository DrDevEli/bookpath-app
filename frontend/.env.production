# BookPath Frontend — production build settings
#
# Source maps are OFF: the production bundle is served publicly, and a .map file
# is a complete, readable copy of the app's source (the 2.7 MB main.*.js.map was
# downloadable from bookpath.org/static/js/). Build with maps locally when
# debugging; never ship them.
GENERATE_SOURCEMAP=false
